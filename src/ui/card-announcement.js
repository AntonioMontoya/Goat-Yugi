export function renderCardAnnouncementModal({ view, esc }) {
  const options = view?.announcement?.options ?? [];
  if (!options.length) return "";
  return `<div class="card-selection-modal announcement-modal" data-testid="card-announcement-modal" role="dialog" aria-modal="true" aria-label="Declarar una carta"><div class="selection-modal-panel"><div class="selection-modal-head"><div><span>DECLARACIÓN · JUGADOR ${(view.priorityPlayer ?? 0) + 1}</span><h2>Declara una carta</h2><p>Busca en todo el catálogo permitido por OCGCore; ninguna opción legal queda oculta.</p></div><strong class="selection-progress">${options.length} VÁLIDAS</strong></div><label class="announcement-search"><span>BUSCAR POR NOMBRE O PASSCODE</span><input type="search" data-announcement-search autocomplete="off" placeholder="Escribe el nombre de la carta…" /></label><select class="announcement-options" data-announcement-select size="10" aria-label="Cartas que se pueden declarar">${options.map((option) => `<option value="${option.runtimeCode}" data-search="${esc(`${option.name} ${option.runtimeCode}`.toLocaleLowerCase("es"))}">${esc(option.name)} · ${option.runtimeCode}</option>`).join("")}</select><div class="selection-modal-actions"><small data-announcement-count>${options.length} cartas coinciden.</small><button type="button" class="primary-button selection-confirm" data-announcement-confirm>Declarar carta</button></div></div></div>`;
}
